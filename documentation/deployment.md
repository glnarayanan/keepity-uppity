# Deployment

This project is a single static HTML file.

## Local
- Open `index.html` in any modern browser.

## Static Hosting
- Serve the repository as static files (any static host works).
- Set the entry point to `index.html`.
